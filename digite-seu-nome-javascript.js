let nome = prompt("Digite seu nome: ");
let sobreNome = prompt("Digite seu sobrenome: ");
let nomeCompleto = alert ((nome)+(sobreNome));

alert("Confirme para gerar seu nome catálogo");
alert(`Seu nome catálogo é: ${sobreNome.toLocaleUpperCase()}, ${nome}`);